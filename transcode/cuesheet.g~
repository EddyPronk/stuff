parser cue:

  ignore:        '\\s'
  token END:     "$"
  token NUM:     '[0-9]+'
  token KEYWORD: '[A-Z]+'
  token STR:     '"([^\\"]+|\\\\.)*"'

  rule track : KEYWORD
        (
            NUM {{ return (KEYWORD, string.atoi(NUM)) }}
          | STR {{ return (KEYWORD, STR) }}
        )

  rule sheet : {{ result = [] }} 
        (
            track {{ result.append(track) }}
        )*
        END {{ return result }}

